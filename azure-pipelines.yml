trigger:
- none

pool:
  name: irfank
  
steps:
- task: FortifySCA@7
  inputs:
    applicationType: 'dotnet'
    fortifyProjects: 'msbuild /t:rebuild RichesDotnetApp.sln'
    fortifyBuildId: 'test2'
    scaVerbose: true
    scaDebug: true
    fortifyScanType: 'LocalScan'
    runFortifyUpload: true
    fortifyServerName: 'fortifyssc'
    fortifyApplicationName: 'deneme'
    fortifyApplicationVersion: 'kotlin'
